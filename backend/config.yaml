server:
  port: "8080"
  mode: "debug"  # debug 或 release

mongodb:
  uri: "mongodb://192.168.31.12:27017"
  database: "imsystem"

jwt:
  secret: "your-secret-key-here"  # 请在生产环境中修改为更安全的密钥
  expiresIn: 24  # token过期时间（小时）

ai:
  apiKey: "sk-78305562a4c942748da616a3d9a58ad7"  # Gemini API密钥
  # model: "gemini-pro"  # AI模型名称
  url:  "https://api.deepseek.com/chat/completions"

file:
  basePath: "./uploads"  # 文件上传路径